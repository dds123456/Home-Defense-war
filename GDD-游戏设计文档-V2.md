# 《Home Defense War / 家园保卫战》游戏设计文档（GDD）

> 版本：V2.0 · 重写日期：2026-08-15 · 状态：**设计稿（待逐子系统评审）**
> 重写基线：以一份"3A 卡通渲染竞速游戏"的标准 GDD 的**精细度**为范本，保留本作（3D 米哈游风塔防）的全部玩法，把文档精度拉到同一档。
> 精度铁律：每个技术点**有名、有方法、有阈值、有陷阱**；性能是一等公民；视觉结论必须靠**真实截图**验证；质量**诚实分级**；数值分三类——`有 rationale` / `[PLACEHOLDER]` / `待验证`。

---

## 0. 精度标准：本 GDD 为什么这样写

参考范本要求的所有"不可妥协项"，本作一一对应落地：

| 范本要求 | 本作对应 |
|---|---|
| 技术栈 Vite + Three.js + TS + ESM，`npm i && npm run dev` 即玩 | 同左（当前 V1.7 为 r160 + 原生 JS 单文件打包，V2 迁移到 Vite + TS + ESM，见 §1） |
| 不使用任何外部素材 | 同左：所有网格程序化、所有纹理 Canvas2D/着色器噪声、所有音频 Web Audio 合成 |
| 目标硬件 + 60fps Retina | §1.3：PC/中端移动端、桌面 Retina 60fps、移动 30–60fps |
| 美术方向是核心（NPR，零 PBR） | §2：**整篇文档的心脏** |
| 把"水面"当 3A 独立问题给最多迭代 | §3：把"角色/怪物程序化绑定动画" + §4"VFX 战斗反馈"当 3A 独立问题 |
| 子代理并行 + 截图测试工具 + 视觉评审 | §11：开发方法论与质量验证标准 |
| 按可游玩里程碑交付 | §11.4：8 个可游玩里程碑 |
| 诚实记录质量（70% 就说 70%） | §13：诚实质量记录 |

**本作的 Fun Hypothesis（一句话，地基）：** 好玩的核心是——**"在有限的金币与棋盘上，用布局决策 + 时机释放，把一波波怪物潮水挡在家园水晶之外，并看着自己的卡通小人和塔打出干净利落的打击感。"**

**Design Pillars（不可妥协的玩家体验，之后每个决策都用这 4 条过审）：**
1. **布局即策略**：全图可建造 + 射程可视化，决策自由度高于固定塔位；任何削弱"看清-决策-落子"闭环的设计都违反本柱。
2. **米哈游风卡通表达**：阶梯光照 + 墨线描边 + 高饱和有限色板；任何表面看起来像 PBR 写实 = 美术方向失败。
3. **打击感可信**：受击闪光、击杀粒子、开火、范围圈、BOSS 预警——每个反馈都要"硬"，不能是装饰性贴图。
4. **低门槛高深度**：自动战斗 + 手动开波 + 简单操作；但双路径、飞行克制、BOSS 阶段、属性克制提供策略深度。

---

## 1. 不可妥协的约束

### 1.1 品类 / 平台 / 目标用户 / 单局时长（设计四要素，缺一不可）

| 项 | 取值 |
|---|---|
| 品类 | 3D 米哈游风**卡通渲染塔防**（单机、网页） |
| 平台 | PC 浏览器（Chrome/Safari/Edge）+ 移动端浏览器（iOS Safari / Android Chrome） |
| 目标用户 | 18–35 岁塔防核心玩家；二次元/米哈游风美术爱好者；休闲策略玩家 |
| 单局时长 | **15–25 分钟**（准备 1–2 分钟 + 20–28 波防守 + 结算） |

> 若实测单局偏离此区间，整条波次曲线与金币产出要重画（见 §5.5 rationale）。

### 1.2 技术栈（硬性）

- **Vite + Three.js（最新 r1xx）+ TypeScript + ES Modules。**
- 用户只需两条命令即可进入游戏：`npm install` → `npm run dev`，浏览器自动打开即玩。
- **不引入任何额外框架**（无 React/Vue/游戏引擎封装）。UI 用原生 DOM + CSS（卡通风格自绘，见 §8），不引 UI 库。
- 不留下任何"需要你排查的构建步骤"。构建配置（vite.config.ts）必须在仓库内、开箱即用。
- 当前 V1.7 的 `esbuild 单文件打包 + file 直开` 作为**离线分发**分支保留（见 §12），但**开发/游玩主路径走 Vite dev server**。

### 1.3 零外部素材（硬性，与范本同级）

- **网格**：全部通过代码创建（程序化几何体 / BufferGeometry）。角色、怪物、塔、水晶、地形、云均不依赖任何 `.glb/.obj/.png` 模型文件。
- **纹理**：全部在代码中生成——Canvas 2D 绘制（色带、UI、墨线蒙版）或着色器噪声（水面/地形细节）。禁止加载 HDRI/贴图/法线图。
- **音频**：全部用 Web Audio API 合成（振荡器 + 噪声 + 包络 + 滤波）。禁止加载 `.mp3/.wav`。
- 资源清单（V1.7 `assets/` 目录）中的参考图**仅作美术对照，不进构建**（见 §12 白名单）。

### 1.4 目标硬件与性能预算（一等公民，不是最后再想）

| 目标 | 桌面 | 移动端 |
|---|---|---|
| 设备 | 主流 PC（M 系 / x86 独显或核显） | 中端手机（A15 级 / 骁龙 8 Gen2 级） |
| 分辨率 | Retina（devicePixelRatio 上限 2） | 上限 2，默认按屏幕实际 DPR |
| 帧率 | **稳定 60fps** | **30–60fps**（按档位） |
| 浏览器 | Chrome 优先，Safari 兼容 | Safari / Chrome |

**性能硬指标（任何里程碑都不得突破）：**
- 同屏 **60+ 怪物 + 全特效** 时，桌面 ≥ 60fps，移动端 ≥ 30fps。
- **Draw Call 预算**：桌面 ≤ 200，移动 ≤ 120（含后处理）。超出 = 必须用实例化/合批消化。
- **几何体预算**：单只怪物 ≤ 1.2k 三角面（LOD0）；塔 ≤ 2k；水晶 ≤ 3k。
- **粒子预算**：同屏活跃粒子 ≤ 4000（桌面）/ 1500（移动），单粒子 < 1 draw call（GPU 粒子或 InstancedMesh）。
- **首屏到可操作 ≤ 3 秒**（含首帧着色器编译）。

> 性能本身就是质量要求。画面再漂亮，只要卡顿，就是失败的游戏。§10 给出达成手段。

---

## 2. 美术方向——整个项目的核心

> 这是整篇文档的心脏。整款游戏采用 **NPR（非真实感渲染）+ 日式/米哈游动画卡通风格**。参考：**《原神/崩坏》**的明亮卡通体积感与二次元比例、**《罪恶装备 Xrd》**的墨线表现、**《Arcane》**的渐变分层着色。完全不要 PBR 写实感。如果最终游戏里任何表面看起来像基于物理的真实材质，美术方向即失败。

### 2.1 渐变阶梯光照（量化漫反射——整体视觉最重要的部分）

- 使用 **MeshToonMaterial + 自定义 gradientMap**，或经过大幅扩展的 ShaderMaterial。
- **色带 3–4 档**，纹理用 `NearestFilter`、**不做插值**（硬切换）。档位阈值初始建议 `[0.35, 0.6, 0.85]`，但**必须按实际画面调**，不直接用默认值。
  - 暗部档：环境光遮蔽后的基础色（约 55% 亮度）
  - 中间档：主光半亮（约 78%）
  - 亮部档：主光全亮（100%）
  - 可选第 4 档：高光预备（112%，仅角色金属/水晶）
- **陷阱**：默认 `gradientMap` 在 0/1 端点会采样到错误像素 → 色带两端留 1px 安全边；`magFilter`/`minFilter` 必须同为 `NearestFilter`，否则远处出现渐变带。
- **陷阱**：方向光强度过高会压垮中间档 → 主光强度锁在 `[0.8, 1.1]`，环境光占 `0.35–0.45`，避免亮部糊成一片。
- 应用对象：塔、英雄、怪物、基地水晶、地形。

### 2.2 倒置外壳描边（Inverted Hull）

- 对**英雄、怪物、塔、水晶**复制网格，沿**平滑法线**向外推顶点，`side = BackSide`，黑色/深色墨线。
- **屏幕空间恒定线宽**：外推距离 = `baseWidth * (projectionFactor / distance)`，按观察距离缩放，让线在屏幕空间宽度恒定——近处不粗得夸张，远处不消失。
  - 公式建议：`offset = clamp(lineWidthPixels * 0.5 * (focalLength / viewZ), minPx, maxPx)`，单位为裁剪空间。
- **陷阱**：硬边几何体（Box/Cylinder 默认法线）推出来是锯齿方块 → 描边网格必须用**平滑/合并法线**（或对低面模型做 `computeVertexNormals` + 法线平均）。
- **陷阱**：描边与本体 z-fighting → 本体材质 `polygonOffset` 或描边用微小 depth bias。

### 2.3 屏幕空间边缘检测后处理（补充内部线条）

- 用 **EffectComposer**：先渲染 **MRT**（法线 + 深度）预处理通道，再跑 **Sobel** 滤波器提取内部轮廓。
- 用途：倒置外壳**无法**生成的**内部线条**（角色四肢分界、怪物护甲板缝、塔的层级结构）。
- **两套线协同**：Sobel 阈值调高，只描"真内部边"，避免与外轮廓**重复叠加**导致线条发胖。
  - 协同策略：外轮廓 = 倒置外壳（实、粗）；内部线 = Sobel（细、半透明），二者在合成阶段用 `max` 而非 `add`。
- **陷阱**：深度不连续处（远处地平线）Sobel 全亮 → 用法线差为主、深度差为辅，并对远处做 fade。

### 2.4 Fresnel 边缘光

- 每个角色/塔/水晶加 **Fresnel rim**：`rim = pow(1.0 - dot(N, V), rimPower)`，叠加在色带之上。
- 目的：让轮廓从水面/地形背景中**清楚凸显**（尤其怪物群在暗色亡灵章节）。
- 颜色取色板中的"高光暖白"或章节强调色，强度 `0.15–0.35`，`rimPower ≈ 3.0`。

### 2.5 分段高光（Segmented Specular）

- 高光**硬边**，不平滑衰减。用 **Matcap** 或色带模拟环境反射，**不用真实立方体贴图反射探针**。
- 金属/盔甲/水晶：Matcap 取"上亮下暗"的卡通球，采样后做 **2–3 档量化**切割，形成硬高光。
- **陷阱**：Matcap 不随光照旋转 → 角色转身时高光不动会"假"，需把 Matcap 视空间方向与主光方向做一次轻混合。

### 2.6 天空与大气（3 章主题）

- **渐变天空穹顶着色器**（顶暗底亮或反之，按章节）。
- **风格化卡通云朵**：扁平形状 + 硬边轮廓，缓慢飘动（顶点/UV 偏移，周期 ≥ 40s）。
- **太阳/月亮光晕**：图形化泛光（径向渐变硬环），非摄影写实 bloom。
- **统一有限高饱和色板**（见附录 B），**水面（地形）、天空、塔体、UI 共用同一套**，避免"各画各的"。

| 章节 | 主题 | 天空 | 主色板强调 | 情绪 |
|---|---|---|---|---|
| 一 边境防线 | 明亮草原/山口 | 蓝→白渐变 | 草绿 / 天蓝 / 暖金 | 轻快、希望 |
| 二 亡灵荒地 | 阴森墓地/湖泊 | 灰紫→墨绿 | 骨白 / 幽紫 / 毒绿 | 压抑、警觉 |
| 三 魔焰深渊 | 灼热熔岩 | 暗红→橙黑 | 熔红 / 炭黑 / 硫黄 | 压迫、终局 |

### 2.7 美术方向失败判据（评审用）

- 任何表面出现镜面模糊衰减高光 / 环境反射模糊 → 失败。
- 描边在近处粗成黑块、远处消失 → 失败。
- 角色边缘 Fresnel 缺失、融进背景 → 失败。
- 三章色板不统一、UI 与场景撞色 → 失败。

> **实现级落地参数**（精确色值、描边世界单位公式、Sobel 阈值、GLSL 片段）见同级文件 **`美术渲染规格书-Shader与描边参数.md`**。该规格书是本条的直接实现附录，程序员照着写即可，禁止再"凭感觉调"。

---

## 3. 角色与怪物——把它当成 3A 级独立问题

> 范本把"水面"列为最难的独立系统、给最多迭代。本作对应的最难系统是**角色与怪物的程序化绑定与动画**——它占屏幕大量面积、直接决定"米哈游风"是否成立、也是最容易被做成"T Pose 人形雕像"翻车的地方。给这个子系统最多的迭代次数。

### 3.1 程序化几何体与绑定（全部代码生成）

- **人形结构**：头 / 躯干 / 上臂×2 / 前臂×2 / 大腿×2 / 小腿×2 / 脚×2 /（怪物含尾巴、翅膀、四足）。全部用基础几何体（Capsule/Box/Cylinder/Sphere）按骨骼层级组装成 Group。
- **绑定**：代码建立 **骨骼层级树**（不是 Skeleton 蒙皮，而是 Group 嵌套 + 局部旋转），每根"骨"挂子网格。动画 = 逐骨 `rotation`/位置关键帧。
  - 理由：塔防同屏 60+ 单位，蒙皮骨骼开销大；Group 嵌套 hierarchy 可用**实例化 + 共享姿态**压成本（见 §10）。
- **二次元比例**：英雄头身比 ≈ 1:6.5（大眼小脸），怪物按类型缩放（普通兵 ≈ 塔高一半）。

### 3.2 拟人动画系统（与船只同权——本作是与塔同权）

每个角色必须有**活**的动作，禁止 T Pose 雕像、禁止僵硬装饰人偶。

| 状态 | 表现 | 驱动 |
|---|---|---|
| 待机 | 随"虚拟波浪/呼吸"轻微上下浮动 + 躯干微摆（周期 ~3s） | 正弦 + 噪声 |
| 行走/移动 | 摆臂 + 迈腿 + 身体起伏；四足怪**对角步态**；飞行怪**扇翅** | 相位错位正弦 |
| 攻击/普攻 | 手臂前挥 / 法杖举起的预备-释放-收招三段 | 状态机 + 时长曲线 |
| 受击 | 身体后仰 + 闪白（见 §4） + 短暂硬直 | 受击事件触发 |
| 死亡 | 倒地/碎裂 + 击杀粒子，非瞬删 | 死亡状态机 |
| 技能 | 英雄主动技演出（游侠拉弓蓄力、法师举杖引燃） | 技能 CD 触发 |
| 庆祝 | 通关后英雄举手/跳跃（结算镜头用） | 胜利事件 |

- **转弯/侧移倾斜**：英雄移动转向时躯干 `roll` 倾斜，幅度随转向角速度。
- **重心转移**：加速前倾、刹车后仰（`pitch`），幅度随速度差。
- **落水/受击下蹲缓冲**：本作无落水，但英雄被 BOSS 冲击波命中时做下蹲硬直。

> **失败信号**：评审截图里任意角色看起来像"站桩道具"或"T Pose 雕像" → 该子系统判失败，回到 §3.2 重做。

### 3.3 怪物分型与 16 种实现

| 分型 | 成员 | 动画要点 |
|---|---|---|
| 人形地面 | 哥布林/兽人/骷髅/僵尸/恶魔小鬼/重甲恶魔 | 摆臂迈腿，重甲加"顿挫"步态 |
| 四足 | 狼骑兵/地狱犬/巨魔(半直立) | 对角步态，狼骑兵带骑手剪影 |
| 飞行 | 石像鬼/幽灵/双足飞龙 | 扇翅 + 悬浮漂移，幽灵半透明 |
| 精英 | 狼骑兵/暗影刺客/巨魔/巫妖/石傀儡/重甲恶魔 | 体型放大 1.3–1.8×，带专属配色 |
| BOSS | 兽人队长/骷髅王/地狱魔像 | 见 §3.4 |

### 3.4 BOSS 机制与预警（关底压迫感）

每个 BOSS 必须有**可学习的 telegraph（预警）→ 执行 → 收招**三段，且**半血转阶段**（V1.7 改进意见 #2 已提，V2 强制落地）：

| BOSS | 技能 | 预警 | 阶段切换 |
|---|---|---|---|
| 兽人队长 | 震地眩晕（范围 3 格） | 地面裂纹圈 + 1s 蓄力 | 半血：增加冲刺践踏 |
| 骷髅王 | 召唤 2 骷髅（上限 6） | 法阵亮起 0.8s | 半血：召唤 + 骷髅狂暴 |
| 地狱魔像 | 火焰冲击波（降周围塔 50% 攻速 3s） | 全屏红闪 2s | 半血：双波 + 熔岩喷发 |

- **预警圈** = 风格化硬边圆环（见 §4），让玩家有"看得见的危险"。
- **失败信号**：BOSS 战"只是血厚的小兵"、无预警、无阶段 → 失败。

---

## 4. VFX 战斗反馈系统——风格化打击感

> 范本把"泡沫"列为风格化水面可信度的关键、要求是一套**真正的系统**而非贴花。本作对应物是**战斗反馈 VFX**——它是"米哈游风打击感"的关键，必须是一套系统，不能只是零星粒子。

### 4.1 反馈通道（先查通道，再怀疑机制）

"感觉不对"先查**视觉/音频/触觉（屏震）/UI** 四通道是否到位，再怀疑数值本身（见 §11 评审纪律）。

### 4.2 必须实现的反馈系统

| 反馈 | 实现 | 要求 |
|---|---|---|
| 受击闪光 | 命中瞬间目标 `emissive` 闪白 80–120ms | 硬切，不渐变 |
| 击杀粒子 | 碎裂/爆裂粒子（InstancedMesh） | 颜色取怪物主色，向四周炸开 |
| 开火 muzzle | 塔口闪光 + 弹道拖尾 | 硬边光点，非模糊辉光 |
| 范围圈 | 塔射程/技能范围可视化 | 半透明描边圆环，建造时显示 |
| BOSS 预警圈 | 硬边预警（裂纹/法阵/红闪） | 倒计时可读，≥0.8s |
| 基地受击 | 水晶闪红 + 屏震 | 每次漏怪触发 |

### 4.3 粒子预算与性能

- 同屏活跃粒子 ≤ 4000（桌面）/ 1500（移动），单粒子 < 1 draw call。
- 用 **GPU 粒子 / InstancedMesh** 而非每个粒子一个 Mesh（见 §10）。
- 击杀粒子对象池化，死亡动画结束即回收。

---

## 5. 游戏内容

> 以下全部基于 V1.7 已配置数值，重写为 `purpose / inputs / outputs / edge cases / 失败态`，并为关键数值补 **rationale**（未验证的标 `[PLACEHOLDER]`）。

### 5.1 战斗系统

- **Purpose**：把"怪物沿路推进 vs 塔/英雄拦截"做成可读的攻防。
- **Inputs**：玩家建塔/升级/出售、开波、英雄移动/技能、道具。
- **Outputs**：怪物死亡/漏怪、基地扣血、金币结算。
- **伤害公式**（保留 V1.7，补 rationale）：
  - 物理伤害 = `攻击 × (1 - 护甲/(护甲+100))` —— rationale：护甲 100 时减伤 50%，曲线在 0–100 区间陡峭、之后平缓，避免堆甲无解。
  - 魔法伤害 = `攻击 × (1 - 魔抗/(魔抗+100))` —— 同形，保证物理/魔法对称可读。
  - 最终伤害 ±10% 浮动，最低 1 点（rationale：避免"差一点没打死"的挫败，同时保留随机张力）。
- **状态效果**：减速/冻结/眩晕/灼烧/破甲；BOSS 免疫眩晕/冻结；飞行怪免疫地面减速。
- **Edge cases**：怪物同时踩多个减速 → 取**最强**而非叠加（防秒杀）；冻结与减速共存 → 冻结优先。
- **失败态**：基地生命 ≤ 0 → 本关失败。

### 5.2 防御塔系统（4 种 × 3 级）

| 塔 | 定位 | Lv1→Lv3 核心特性 | 目标优先级 |
|---|---|---|---|
| 箭塔 | 单体/对空 | 穿透 1 → 多重 2 目标 | 最前方 |
| 魔法塔 | 高伤/亡灵 | 0.5 格溅射 → 命中减速 15%/2s | 血量最高 |
| 炮塔 | 群体 AOE | 破甲 30% → 灼烧 8/s×3s | 最密集区域 |
| 冰霜塔 | 控制 | 减速 45%+10%冻 → 每 4s 范围冻结 1s | 最前方 |

- **升级**：最高 3 级；攻击间隔 = `1/攻速 + 蓄力`；出售返还累计投入 **70%**（rationale：允许试错重布局，但不鼓励无脑翻建）。
- **Rationale 缺口**：Lv1→Lv2→Lv3 的伤害/造价曲线是否平滑，需 §11 数值模拟验证 → 标 `[PLACEHOLDER · 待 DPS 模拟]`。
- **Edge cases**：塔满级仍被点升级 → 禁用按钮；范围圈与另一塔重叠 → 允许（策略层）。

### 5.3 怪物与 BOSS（16 种 + 3 BOSS）

- 数据见 `游戏数据.md` §3（已完整）。V2 补：
  - **章节缩放**：第二章 ×1.25、第三章 ×1.65；每 5 波 +8% —— rationale：保证后期压力，但 `×(1+0.08×floor(wave/5))` 需验证不与经济脱节（见 §5.5）。
  - **"掉血"字段**：V1.7 改进意见 #2 指出"掉血"语义不清 → V2 统一为**基地泄漏伤害值（PRD）**，已对齐（哥布林漏=1，BOSS 漏=10–12）。
- **飞行怪**：仅箭塔/魔法塔/英雄技能可攻击（rationale：制造"对空决策"，避免地面塔无用）。需补**有效对空 DPS 指标**验证 3005 关（§11 校验）。

### 5.4 英雄系统（2 名）

| 英雄 | 定位 | 主动技 | 解锁 |
|---|---|---|---|
| 王国游侠 | 物理远程/对空 | 穿透箭（CD15s，对飞行+50%） | 初始 |
| 宫廷法师 | 魔法范围/控制 | 火焰雨（CD20s，灼烧3s+减速20%） | 完成 3003 |

- 点击选中 → 点地图全图移动（自动寻路绕塔）→ 遇怪自动普攻。
- 局外升级：每级 +2% 攻击、+1% 移速，上限 30 级；费用 `30 + 当前等级×20` 局外金币。
- **改进（V1.7 #3）**：V2 规划**英雄天赋分支**（站桩高伤 / 游走支援）作为后期扩展，不在首版强制。

### 5.5 经济系统（Sources & Sinks 建模）

> 用一张图回答：金币从哪来、到哪去、不同档玩家够不够花。任何 source 找不到对应 sink = 通胀定时炸弹。

**Sources（流入）：**
- 初始金币（每关 200–400，随章节升）
- 击杀金币（按怪物索引即时发放）
- 波次奖励 `30 + 波次×5`
- 提前开波 `剩余秒数×2`
- 道具/技能（金币雨 +200）

**Sinks（流出）：**
- 建塔（50–100）/ 升级（50–200）
- 商店技能（40–80 局外金币购买，局内消耗库存）
- 英雄升级（局外金币）

**通胀防护（硬约束）：**
- **经济回收率目标**：单关总收入 ÷ 最低通关成本 ≥ **1.3×**（V1.7 改进 #5）→ `[PLACEHOLDER · 待模拟]`。
- 局外金币**不回流局内**（只买技能库存），避免长线通胀。
- **失败信号**：某关回收率 < 1.0 → 玩家必败，经济模型失败。

### 5.6 关卡设计（3 章 8 关）

| 章节 | 关 | 波 | 核心机制 | 路线设计要点 |
|---|---|---|---|---|
| 一 | 3001 | 20 | 新手教学 | 单路宽直道，前 3 波慢速兵教学建塔 |
| 一 | 3002 | 22 | 双路径+精英 | A/B 路奇数/偶数，逼出对空塔 |
| 一 | 3003 | 25 | BOSS 兽人队长 | 路末开阔地放 BOSS 战 |
| 二 | 3004 | 21 | 亡灵主题 | 引入亡灵克制（魔法塔加权） |
| 二 | 3005 | 23 | 双路径+大量飞行 | 强制对空，验证飞行 DPS |
| 二 | 3006 | 26 | BOSS 骷髅王 | 召唤机制 + 半血阶段 |
| 三 | 3007 | 24 | 双路径+高护甲精英 | 破甲（炮塔）成为必需 |
| 三 | 3008 | 28 | BOSS 地狱魔像 | 终局高压，全机制综合 |

- **路线**：用 `CatmullRomCurve3` 定义怪物路径（地面/飞行可不同高度曲线），渲染为贴地发光引导带（与范本赛道同理，但本作是"敌进路"而非"赛车道"）。
- **双路径**：3002/3005/3007 奇数波走 A、偶数波走 B，逼玩家覆盖两条线。

### 5.7 局外成长与运营

- 局外金币：胜利 `20+星级×10+首通50`；失败 `5+波次`。
- 体力：上限 20，进关 -5，每 5 分钟 +1（rationale：限制单日挑战量，制造回访；V1.7 #6 建议补每日赠送作为后期）。
- 商店：箭雨/火球/治疗波/时间缓速/金币雨（局外金币购买，局内按库存用）。
- 存档：关卡解锁/星级/英雄等级/金币/体力/技能库存本地持久化。

---

## 6. 敌人 AI 与波次逻辑

- **路径跟随**：怪物沿 `CatmullRomCurve3` 采样点前进，用**前瞻点**（lookahead）做转向，避免拐点穿模。
- **步态驱动**：移动速度 → 动画相位速度（见 §3.2），速度变化平滑（帧率无关）。
- **飞行**：走独立高空曲线，地面减速/冰霜地面效果免疫。
- **双路径调度**：波次控制器按奇偶分配 A/B 路，需保证两路**总压力均衡**（rationale：避免一路白建）。
- **BOSS AI**：沿路慢速推进 + 周期性释放 telegraph 技能（§3.4），半血触发阶段。
- **轻度橡皮筋**（V1.7 无，V2 可选）：怪物群整体推进速度对"玩家漏怪数"做轻微补偿，保持张力但不保送。
- **Edge cases**：路径点丢失 → 回退上一有效点；怪物叠加 → 轻微分离力避免完全重合。

---

## 7. 相机系统

- **战术俯视相机**：弹簧阻尼跟随基地/战场中心，可拖拽旋转、滚轮/双指缩放（V1.7 操作表已有）。
- **速度/张力联动**：英雄高速移动或 BOSS 战时轻微拉远 + FOV 微增，增强压迫。
- **屏震**：基地受击、BOSS 冲击波命中时轻微震动（<150ms，衰减快）。
- **电影化镜头**：通关结算用环绕镜头扫过水晶与英雄（对应范本"倒计时/结算环绕"）。
- **陷阱**：旋转后法线/描边计算需重算视空间 → 相机矩阵变更时同步刷新 MRT。

---

## 8. HUD / UI（卡通风格，非默认 HTML）

- 与游戏一致卡通风：**生命（基地水晶）/ 金币 / 波次 / 排名（本作=波次进度）/ 英雄技能 CD / 加速槽（本作=技能库存）/ 小地图**。
- 小地图：显示路径样条 + 全部怪物 dot + 塔位 + BOSS 预警。
- **界面专门设计**：自绘 DOM/CSS 组件（圆角、描边、高饱和色板），**不引 UI 库、不用裸默认元素**。
- **失败信号**：HUD 与场景撞色、信息读不清、移动端热区小 → 评审判失败。

---

## 9. 音频（Web Audio 合成）

全部代码生成，无音频文件：
- **引擎/攻击音**：振荡器随攻速变音高（塔射击、英雄普攻）。
- **水流/环境**：滤波白噪声随波次密度增强（本作=战场环境音随怪物数增强）。
- **受击/击杀/撞击**：短促包络 + 噪声爆裂。
- **起跑/波次号角**：合成号角（多振荡器叠加）。
- **章节自适应音乐**：3 章各有 BPM/情绪曲线 loop（V1.7 资源清单已规划）。
- **陷阱**：Web Audio 需用户手势后 `resume()`，首次交互前静音。

---

## 10. 性能架构（达成 §1.4 的手段）

| 手段 | 用途 | 说明 |
|---|---|---|
| **实例化渲染** | 同型怪物/塔部件/粒子 | InstancedMesh，姿态用实例属性 |
| **视锥体剔除** | 屏外单位不绘制 | 每帧八叉树/包围球测试 |
| **自适应像素比** | Retina 不爆 GPU | `pixelRatio = min(devicePixelRatio, 2)`，移动降档 |
| **LOD** | 远处减面 | 怪物/塔 3 档（LOD0/1/2），切换无跳变 |
| **对象池** | 弹道/粒子/伤害数字 | 预分配，死亡即回收，零 GC 尖峰 |
| **合批/共享材质** | 压 Draw Call | 同材质合并，gradientMap/Matcap 共享 |
| **MRT 一次出** | 后处理 | 法线+深度一次渲染，供 Sobel 用 |

- **Draw Call 预算**：桌面 ≤200、移动 ≤120（含后处理），超出即合批/实例化消化。
- **低端机实测**：V1.7 改进 #9 要求——60+ 怪 + 全特效帧率需在真机验证，非纸面。

---

## 11. 开发方法论与质量验证标准

> 范本的核心工作纪律：先定义共享架构 → 子代理并行 → **截图测试工具** → **视觉评审代理** → 逐子系统迭代到无明确缺陷 → 按可游玩里程碑交付 → **诚实质量记录**。本作照此执行。

### 11.1 共享架构先行
先冻结：坐标系/单位（1 格 = 世界单位）、事件总线、材质注册表（gradientMap/Matcap/色板单例）、对象池接口、MRT 渲染管线。子代理基于此并行。

### 11.2 截图测试工具（先于其他内容建立）
- 用 **Playwright + 无头浏览器**，加载游戏、运行到指定时刻（如"第 5 波""BOSS 半血""结算"），从**多个相机角度**以 **Retina 分辨率**截图。
- 所有视觉结论**必须靠真实截图**，不靠代码推测。
- **已落地框架**：`tools/screenshot-harness/`（含 `harness.mjs` / `scenarios.mjs` / `screenshot.config.mjs` / `demo/game-mock.html` / `README.md`）。`npm run shot:mock` 开箱即跑通管线；真实游戏实现 `window.__GAME_TEST__` 契约（`seek(moment)` + `setCamera(preset)`）后即可 `npm run shot`。详见该目录 README。

### 11.3 视觉评审代理（专门挑刺）
唯一职责：严格看截图，判"这一帧能否达到已发行的米哈游风塔防水平？"指出具体缺陷：
- 色阶阈值导致画面浑浊 / 描边在角色边缘断裂 / 角色像道具 / 粒子静止不动 / 三章色板不统一。
- **职责是报告问题，不是为友好而批准**。每个子系统对评审意见循环迭代，直到评审提不出明确缺陷。

### 11.4 可游玩里程碑（每步交付可运行版）
1. **场景 + 相机 + Toon 管线**：空地图 + 战术相机 + 阶梯光照/描边跑通。
2. **单塔 + 单怪 + 建造/攻击手感**：1 塔 1 怪，建塔/射程圈/受击反馈。
3. **卡通管线 + 描边应用于全部对象**：塔/怪/英雄/水晶全上墨线 + Fresnel + Matcap。
4. **波次 + 路径 + 双路径 + 基地**：20+ 波推进、漏怪扣血、胜负。
5. **16 怪 + BOSS + 技能**：全怪物分型 + 3 BOSS 阶段 + 预警。
6. **英雄 + 动画**：2 英雄移动/普攻/技能 + 拟人动画。
7. **HUD + 音频**：卡通 HUD + Web Audio 全套。
8. **性能优化 + 最终打磨**：实例化/LOD/对象池达标，评审清零。

### 11.5 诚实质量记录纪律
- 某子系统只到 70% → **明确说 70% 并解释原因**，不伪称完成。
- 能运行只是最低要求，不是终点。

---

## 12. 交付成果

- 含 **README**（两条命令即玩、架构说明、着色器注释）、**架构清晰**、**代码易读**、**着色器有注释**的仓库。
- `npm install && npm run dev` 必须可玩，进入即一场卡通塔防对局。
- 离线分支：`vite build` → 单文件包，`file://` 直开（保留 V1.7 分发能力）。
- **资源白名单脚本**：防止 `assets/` 参考图误入线上包（见 V1.7 资源清单 #5）。
- 所有数值集中单一配置（JSON/TS），便于策划热更与回归（V1.7 改进 #10）。

---

## 13. 诚实质量记录（当前 V1.7 → V2 起点）

> 以下按**文档精度 + 实现成熟度**诚实分级，不夸大。

| 子系统 | 当前水平 | 说明 / 距 V2 标准缺口 |
|---|---|---|
| 玩法框架（塔/怪/英雄/关/经济） | **85%** | 数值完整、可玩；缺 rationale 与 sources/sinks 通胀校验 |
| 卡通渲染管线 | **60%** | V1.7 仅"toon 材质"泛述；缺阶梯阈值、倒置外壳、Sobel、Fresnel、Matcap 的具体实现与评审 |
| 角色/怪物动画 | **55%** | 有摆臂迈腿描述但未落实为代码动画状态机；BOSS 阶段未做；评审未跑 |
| VFX 战斗反馈 | **65%** | 有闪光/粒子/范围圈列表，但是否为"系统"未验证；粒子预算未定义 |
| 相机 | **50%** | 仅旋转/缩放；缺弹簧阻尼、FOV 联动、屏震、结算环绕 |
| HUD/UI | **70%** | 有操作表；卡通自绘组件与移动端热区走查未做 |
| 音频 | **60%** | Web Audio 规划在案，参数表化与章节音乐未落地 |
| 性能 | **50%** | 有合并/雾/对象池；缺实例化/LOD/自适应像素比/DC 预算/真机实测 |
| 验证方法论 | **0%** | 无截图测试工具、无视觉评审代理、无里程碑交付记录 |
| 文档精度（本 GDD） | **从 V1.7 粗放 → V2 本稿** | 本稿即补齐约束/美术核心/系统规格/评审纪律 |

**最需要优先补的三件事（按风险）：**
1. §2 卡通渲染管线真实落地 + 截图评审（美术方向成败在此）。
2. §10 性能架构（实例化/LOD/自适应像素比）在里程碑 1 就接入，不能最后补。
3. §11.2 截图测试工具与 §11.3 视觉评审代理——没有它，所有"视觉达标"都是空话。

---

## 附录 A. 数值 Rationale 速查（逐步填充）

| 数值 | 值 | Rationale | 状态 |
|---|---|---|---|
| 基地生命 | 20–30 | 20 次泄漏=败，容错约 15–19 次 | 待 playtest |
| 出售返还 | 70% | 允许试错布局，但不鼓励翻建 | 已定 |
| 护甲公式 | `/(护甲+100)` | 100 甲=减伤 50%，曲线可控 | 已定 |
| 波次奖励 | `30+波×5` | 后期单波价值随压力升 | `[PLACEHOLDER]` |
| 章节缩放 | ×1.25 / ×1.65 | 后期压力，需经济校验 | `[PLACEHOLDER]` |
| 经济回收率 | ≥1.3× | 防必败，需模拟 | `[PLACEHOLDER]` |

## 附录 B. 统一色板（三章，限色高饱和）

> 具体 hex 在 §2.6 各章主题下扩展；此处为骨架，评审时锁定。

| 角色 | 边境(一) | 亡灵(二) | 魔焰(三) |
|---|---|---|---|
| 天空顶/底 | 蓝 / 白 | 灰紫 / 墨绿 | 暗红 / 橙黑 |
| 强调色 | 暖金 | 幽紫 | 熔红 |
| 墨线 | 近黑 `#1a1420` | 同 | 同 |
| 高光暖白 | `#fff4d6` | 同 | 同 |

---

*本 GDD 为设计稿，所有 `[PLACEHOLDER]` 项须在对应里程碑通过 playtest / 数值模拟 / 截图评审后转正。任何子系统评审未清零前，不宣布完成。*
